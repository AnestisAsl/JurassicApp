import * as React from "react";
import type { NextPage } from "next";
import Head from "next/head";

const Charts: NextPage = () => {
  return (
    <div className="w-screen h-screen flex  flex-col items-center justify-center font-MontserratAlternates">
      <Head>
        <title>Charts</title>
      </Head>
      <h1>welcome charts </h1>
    </div>
  );
};

export default Charts;
